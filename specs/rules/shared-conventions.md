# Shared AI conventions

Synced from **notebox** (canonical). Do not edit synced `.cursor/rules/{language,quality,specs,testing}.mdc`, `.cursor/skills/`, `.claude/settings.json`, or `.claude/hooks/` in this repo.

Full policy and allowlist: see `notebox/specs/rules/shared-conventions.md`.

Re-sync:

```bash
/path/to/notebox/scripts/sync-shared-conventions.sh "$(pwd)"
```
